# Pinned so that rebuilding an old checkout, or upgrading months from now,
# produces the same working stack rather than whatever major version is
# current that day.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
python-multipart==0.0.20
httpx==0.28.1
PyJWT==2.10.1
